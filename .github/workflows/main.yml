name: Test

on:
  workflow_dispatch:

jobs:
  Init-Workflows:
    runs-on: ubuntu-latest

    steps:
        - id: msg
          name: " 👋  - Starting Deployment to AKS"
          run : |
            echo " 👋  - Starting Deployment to AKS"
    

