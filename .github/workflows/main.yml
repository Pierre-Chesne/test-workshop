name: Test

on:
  workflow_dispatch:

jobs:
  Init-Workflows:
    runs-on: ubuntu-latest

    steps:
        - id: msg
          name: " 👋  - Démarrage du Workflow"
          run : |
            echo " 👋  - Démarrage du Workflow"
            
  
  Build-Terraform:
    runs-on: ubuntu-latest
    needs: Init-Workflows
    
    steps:
      - name: Checks-out du repository
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.1.7
          
      - name: Terraform Init
        run: |
          cd ./Terraform         
          terraform init -backend-config="sas_token=${{ secrets.BACKENDSTORAGESASTOKEN }}"
          
      - name: Terraform Validate
        run: |
          cd ./Terraform
          terraform validate
          
      - name: Terraform Plan
        run: |
          cd ./Terraform
          terraform plan \
             -var 'AzureSubscriptionID=${{ secrets.SUBSCRIPTIONID }}' -var 'AzureClientSecret=${{ secrets.CLIENTSECRET }}' -var 'AzureClientID=${{ secrets.CLIENTID }}' -var 'AzureTenandID=${{ secrets.TENANTID }}' -lock=false -out=monplan.tfplan
          ls -lisa
    
    
    
    

